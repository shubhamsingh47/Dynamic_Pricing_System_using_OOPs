# Instead of hard coding values in multiple places, we can keep the settings in one file. If something changes (like the database password), we can update it here.

# There are other ways to achieve this, which will be discussed later.
